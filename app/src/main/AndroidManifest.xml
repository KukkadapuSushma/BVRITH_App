<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bvrit.bvritcall">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="BVRIT HYDERABAD"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="BVRIT HYDERABAD | Login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".DepartmentActivity" />
        <activity android:name=".CseActivity" />
        <activity android:name=".facilities" />
        <activity android:name=".aboutBVRITH" />
        <activity android:name=".mission" />
        <activity android:name=".vision" />
        <activity android:name=".founder" />
        <activity android:name=".chairman" />
        <activity android:name=".principal" />
        <activity android:name=".sves" />
        <activity android:name=".aboutfacilities" />
        <activity android:name=".transport" />
        <activity android:name=".wise" />
        <activity android:name=".library" />
        <activity android:name=".atl" />
        <activity android:name=".hostels" />
        <activity android:name=".canteen" />
        <activity android:name=".EceActivity" />
        <activity android:name=".EeeActivity" />
        <activity android:name=".ItActivity" />
        <activity android:name=".BshActivity" />
        <activity
            android:name=".MainActivity"
            android:label="BVRIT Hyderabad"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity_admin" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".specialLabs"
            android:label="@string/title_activity_special_labs" />
        <activity
            android:name=".incubation"
            android:label="@string/title_activity_incubation" />
        <activity
            android:name=".iot"
            android:label="@string/title_activity_iot" />
        <activity
            android:name=".eclinic"
            android:label="@string/title_activity_eclinic"></activity>
    </application>

</manifest>